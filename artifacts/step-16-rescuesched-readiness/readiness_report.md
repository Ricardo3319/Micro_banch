# Step-16 RescueSched Readiness Diagnosis

Generated by `python scripts/rescue_analysis.py`.

## 1. Project Status Diagnosis

- Language/build: C++17 simulator built by CMake; Python scripts are used for analysis and figures.
- Main entry: `src/app/main.cpp`, executable `build-aqb-check/simulator.exe`.
- RescueSched methods: `M1_RescueSched`, `M1_RescueSched_NoTargetSafety`, `M1_RescueSched_NoRescuable`.
- Workloads currently used by RescueSched: W3 Poisson+Lognormal main, W1 Poisson+Bimodal sanity and overload.
- Existing outputs: step-15 raw CSVs plus this step-16 median table, figures, and readiness report.
- Harmful migration status: predicted target-unsafe accepts and actual target-induced misses are now separated.

## 2. Goal Checklist

| Item | Status | Evidence |
|---|---|---|
| Non-overload SLO gain | satisfied | W3 rho=0.85 SLO: L1 0.20307, M0 0.153639, Rescue 0.094486. |
| Not from more migration | satisfied | Rescue move rate 0.285511 vs M0 0.399815. |
| High BMR | satisfied | Rescue BMR 0.999982. |
| Low actual UMR | satisfied | Rescue UMR 0 with actual harmful semantics. |
| NoRescuable necessity | satisfied | NoRescuable BMR 0.310599, UMR 0.68939. |
| NoTargetSafety necessity | partially satisfied | NoTargetSafety predicted unsafe accepts 8643, but actual harmful 0. |
| Balanced workload non-overreaction | not satisfied | No dedicated balanced low-skew RescueSched run yet; L0 low-rho exists but not a balanced-specialized sanity. |
| Global overload suppress migration | satisfied | W1 rho=0.95 Rescue moves 0. |
| P99/P999 narrative | satisfied | Summary explicitly states RescueSched may raise P99/P999 while lowering SLO violations. |
| Seed stability | satisfied | 10-seed CI table is available in step-17. |
| Parameter sensitivity | satisfied | B=4 BMR 0.947577 and UMR 0.052406. |
| Workload generalization | satisfied | W3 main, W1 sanity/overload, and W2 burst-skew RescueSched are available. |
| Actual target harmful counterfactual | satisfied with model caveat | Implemented watch-based actual target-induced miss; FIFO append produces zero actual harmful in current model. |
| Reproducibility | satisfied | Runner modes and CSV outputs are deterministic by seed. |
| CloudLab readiness | partially satisfied | Ready for trace-driven simulator reproduction and migration-cost microbenchmark, not full user-space prototype claim. |

## 3. Key Corrective Finding

The previous `harmful_migration_count` mixed a predicted target-risk signal into UMR. After the fix, `predicted_target_unsafe_accept_count` records accepted unsafe-target predictions, while `harmful_actual_count` records migrations that actually induced target-side misses.

Under the current append-to-tail FIFO model, actual target-side harm is expected to be zero because an incoming task does not delay tasks already queued ahead of it. The new watch-based counterfactual confirms this in the rerun CSVs. Therefore, target safety is currently only partially validated: it reduces exposure to already risky target queues, but this simulator does not yet demonstrate actual target-induced misses.

## 4. Experiment Interpretation

- W3 rho=0.85: RescueSched SLO violation is 0.094486, better than L1 0.20307 and M0 0.153639.
- W3 rho=0.85: RescueSched move rate is 0.285511, lower than M0 0.399815; the gain is not from moving more tasks.
- NoRescuable: BMR drops to 0.310599 and UMR rises to 0.68939; rescuability is necessary.
- NoTargetSafety: predicted unsafe accepts are 8643, but actual harmful migrations are 0; target-safety evidence needs a stronger model or prototype.
- W1 rho=0.95 overload: RescueSched moves 0 tasks and does not claim to solve global overload.

## 5. Autonomous Task Choice

Chosen tasks:
1. Split predicted target unsafe from actual harmful counterfactual.
2. Add RescueSched-specific analysis and figure generation.
3. Generate a CloudLab readiness report.

Not chosen yet:
- New workload or baseline: lower priority than fixing harmful semantics because the existing claim would otherwise overstate target safety.
- 10 seeds: useful but more expensive; 5 seeds already cover the first local gate.
- Full user-space prototype: should follow CloudLab migration-cost and trace-reproduction checks.

## 6. Generated Figures

- `figures/fig_rescue_slo_vs_rho.png`
- `figures/fig_rescue_slo_vs_rho.pdf`
- `figures/fig_rescue_ablation_quality.png`
- `figures/fig_rescue_ablation_quality.pdf`
- `figures/fig_rescue_budget_sweep.png`
- `figures/fig_rescue_budget_sweep.pdf`

## 7. CloudLab Readiness

Decision: RescueSched is ready for CloudLab Phase 1, not yet for a full user-space prototype performance claim.

Phase 1 should be trace-driven simulator reproduction plus microbenchmarks:
1. Rebuild the simulator on CloudLab and reproduce `rescue-smoke`, `rescue-main`, `rescue-ablation`, and `rescue-overload-sanity`.
2. Measure descriptor-only cross-core enqueue/dequeue cost to replace the current `migration_cost_us=0.5` assumption.
3. Collect or replay per-core arrival/service traces to test whether random-core imbalance and heavy-tail rescue patterns persist.
4. If implementing a user-space prototype, first preserve FIFO append semantics and explicitly log target queue snapshots for harmful attribution.

Blocking caveats before a paper-grade CloudLab claim:
- Step-17 now contains W2 burst-skew, 10-seed CI, and cost/service-estimate calibration for local reporting.
- Need a target-side policy that can actually create target harm, or state clearly that append-to-tail cannot harm existing target tasks.
- Need CloudLab or trace replay before claiming real-system performance.
